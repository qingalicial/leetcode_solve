Determine whether an integer is a palindrome. Do this without extra space.确定整数是否是回文


自己做出来的，主要技巧就是数值转成字符串然后逆序遍历，注意检查极端情况比如0
 def isPalindrome(self, x):
        """
        :type x: int
        :rtype: bool
        """
        if x==0:
            return True
        if `x`== `x`[::-1]:
            return True
        else:
            return False
            
            
 if x < 0: return False
        return x == int(str(x)[::-1])
